# alfaGPT (Volency)

Универсальный бизнес-помощник с ИИ для управления всеми аспектами бизнеса в одном месте. Построен на Next.js 13 с TypeScript, Prisma и PostgreSQL.

## ✨ Реализованные функции

### 🔐 Аутентификация
- Авторизация через email с кодом подтверждения
- Безопасное хранение токенов
- Сессии пользователей

### 💬 Чат с ИИ
- **Общий чат** — универсальный ИИ-помощник для бизнес-задач
- **Юридический чат** — специализированный чат для юридических консультаций
- История всех диалогов с сохранением в базе данных
- Очистка истории чата

### 💰 Финансы
- **Транзакции** — учет доходов и расходов с категориями
- **Платежи** — управление платежами с отслеживанием статусов
- **Регулярные расходы** — настройка периодических платежей
- **Импорт данных** — загрузка транзакций из Excel (XLSX)
- **Экспорт данных** — выгрузка аналитики в Excel
- Визуализация финансовых данных с графиками
- Фильтрация по датам и категориям

### 👥 Сотрудники
- Управление сотрудниками бизнеса
- Учет рабочих дней сотрудников
- Настройка ставок и расписания работы
- История работы по дням

### 📋 Операции
- **Задачи** — управление задачами с приоритетами и дедлайнами
- **Напоминания** — напоминания о важных событиях
- Фильтрация и сортировка задач
- Отслеживание статусов

### 📊 Аналитика и Дашборд
- Обзор всех активностей бизнеса
- Календарь событий и задач
- Визуализация данных
- ИИ-анализ данных

### 🏢 Управление бизнесами
- Создание и управление несколькими бизнесами
- Переключение между бизнесами
- Настройка информации о бизнесе (адрес, карты)

### 📚 API Документация
- Swagger UI для интерактивной документации API
- Автоматическая генерация документации из кода
- Доступна по адресу `/api-docs`

## 🛠 Технологии

- **Next.js 13** — React фреймворк с App Router
- **TypeScript** — строгая типизация
- **Prisma 7** — современный ORM для работы с БД
- **PostgreSQL** — реляционная база данных
- **Chakra UI** — компоненты интерфейса
- **Tailwind CSS** — утилитарная стилизация
- **next-intl** — интернационализация (русский, английский, казахский)
- **react-markdown** — рендеринг Markdown в чате
- **xlsx** — импорт/экспорт Excel файлов
- **swagger-ui-react** — документация API
- **Recharts** — графики и визуализация данных
- **ИИ интеграция** — поддержка нескольких провайдеров (OpenRouter, OpenAI, локальное развертывание)
- **Lucide React** — иконки
- **date-fns** — работа с датами

## 🚀 Быстрый старт

### Требования
- Node.js 20+
- Docker и Docker Compose (для локальной БД)
- Переменные окружения (см. ниже)

### Установка

```bash
# Клонируем репозиторий
git clone <repo-url>
cd alfaGPT

# Устанавливаем зависимости
npm install

# Запускаем PostgreSQL в Docker
npm run db:up

# Генерируем Prisma клиент
npx prisma generate

# Применяем миграции базы данных
npm run db:migrate

# (Опционально) Заполняем тестовыми данными
npm run seed
```

### 📝 Тестовые данные

После выполнения `npm run seed` создаются следующие тестовые пользователи:

**Пользователь 1: `test1@volency.ru`**
- Бизнес: Ларёк и Шаурмечная "Восточная"
- 2 сотрудника, транзакции, платежи, задачи, напоминания

**Пользователь 2: `test2@volency.ru`**
- Бизнесы: 3 пункта выдачи (Центральный, Северный, Южный)
- 4 сотрудника, транзакции, платежи, задачи, напоминания

**Пользователь 3: `test3@volency.ru`**
- Бизнесы: 2 ресторана (Итальянская кухня, Азиатская кухня)
- 5 сотрудников, транзакции, платежи, задачи, напоминания

Для входа используйте любой из этих email-адресов. Код подтверждения можно посмотреть на странице `/{locale}/playwright/authcode` (автообновление каждые 5 секунд).

# Запускаем dev сервер
npm run dev
```

Открой [http://localhost:3000](http://localhost:3000) в браузере.

### 🗄 Управление базой данных

```bash
# Запустить PostgreSQL
npm run db:up

# Остановить PostgreSQL
npm run db:down

# Перезапустить PostgreSQL
npm run db:restart

# Просмотреть логи БД
npm run db:logs

# Применить миграции
npm run db:migrate

# Открыть Prisma Studio (веб-интерфейс для БД)
npm run db:studio

# Сбросить БД (удалить все данные и применить миграции заново)
npm run db:reset
```

### Переменные окружения

Создайте файл `.env` в корне проекта на основе `.env.example`:

```bash
# Скопируйте пример файла
cp .env.example .env

# Отредактируйте .env и заполните свои значения
```

#### Обязательные переменные

```env
# База данных (для локальной разработки через Docker)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/alfaGPT?schema=public"

# Email для отправки кодов авторизации
NAME_EMAIL="your-email@example.com"
PASS_EMAIL="your-email-password"
```

#### Настройка ИИ провайдера

Приложение поддерживает несколько провайдеров ИИ. Выберите один из вариантов:

**Вариант 1: OpenRouter (по умолчанию, рекомендуется)**
```env
AI_PROVIDER="openrouter"
OPENROUTER_API_KEY="sk-or-v1-your-key-here"
OPENROUTER_MODEL="openai/gpt-oss-20b:free"
```

**Вариант 2: OpenAI**
```env
AI_PROVIDER="openai"
OPENAI_API_KEY="sk-your-openai-key-here"
OPENAI_MODEL="gpt-3.5-turbo"
```

**Вариант 3: Локальное развертывание (Ollama)**
```env
AI_PROVIDER="local"
AI_LOCAL_URL="http://localhost:11434"
AI_LOCAL_MODEL="llama2"
```

#### Опциональные переменные

```env
# Telegram уведомления (опционально)
TELEGRAM_BOT_TOKEN="your-telegram-bot-token"
TELEGRAM_CHAT_ID="your-telegram-chat-id"

# URL приложения
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

**Важно:** 
- Для локальной разработки используйте Docker Compose для поднятия PostgreSQL. База данных будет доступна на `localhost:5432` с учетными данными:
  - **Пользователь:** `postgres`
  - **Пароль:** `postgres`
  - **База данных:** `alfaGPT`
- При использовании Docker Compose переменные окружения автоматически загружаются из файла `.env`

## 📜 Скрипты

### Основные команды
- `npm run dev` — запуск dev сервера на http://localhost:3000
- `npm run build` — сборка для продакшена
- `npm run start` — запуск продакшен сервера
- `npm run lint` — проверка кода линтером
- `npm run seed` — заполнение БД тестовыми данными

### Команды для тестирования
- `npm run test:e2e` — запустить все e2e тесты
- `npm run test:e2e:ui` — запустить тесты с UI режимом
- `npm run test:e2e:headed` — запустить тесты в видимом браузере
- `npm run test:e2e:debug` — запустить тесты в debug режиме
- `npm run test:e2e:report` — показать отчёт о тестах

## 🧪 E2E Тестирование

Проект использует Playwright для end-to-end тестирования.

### Структура тестов

```
e2e/
  auth.spec.ts          # Тесты авторизации
  dashboard.spec.ts     # Тесты дашборда
  navigation.spec.ts    # Тесты навигации
```

### Запуск тестов

```bash
# Запустить все e2e тесты
npm run test:e2e

# Запустить тесты с UI (интерактивный режим)
npm run test:e2e:ui

# Запустить тесты в видимом браузере
npm run test:e2e:headed

# Запустить тесты в debug режиме
npm run test:e2e:debug
```

### Настройка

Тесты автоматически запускают dev сервер перед выполнением. Убедитесь, что:
- База данных запущена (`npm run db:up`)
- Переменные окружения настроены в `.env`

### Вспомогательные страницы Playwright

Для удобства разработки и тестирования доступны специальные страницы:

- **Отчёт о тестах**: `/{locale}/playwright/playwright-report`
  - Пример: `http://localhost:3000/ru/playwright/playwright-report`
  - Показывает результаты выполнения e2e тестов

- **Коды авторизации**: `/{locale}/playwright/authcode`
  - Пример: `http://localhost:3000/ru/playwright/authcode`
  - Автообновление каждые 5 секунд
  - Показывает все коды подтверждения, отправленные на email
  - Полезно для тестирования авторизации без проверки почты

### 🌍 Локализация

Приложение поддерживает несколько языков:
- **Русский** (`ru`) — основной язык
- **Английский** (`en`)
- **Казахский** (`kz`)

Язык выбирается автоматически на основе URL (`/ru/...`, `/en/...`, `/kz/...`). Все тексты интерфейса переведены и хранятся в папке `messages/`.

### 📖 API Документация

Интерактивная документация API доступна по адресу:
- `/{locale}/api-docs` (пример: `http://localhost:3000/ru/api-docs`)

Документация генерируется автоматически из кода с использованием Swagger/OpenAPI. Включает все эндпоинты, параметры запросов и примеры ответов.

## 🐳 Docker Deployment

Проект включает Docker Compose конфигурацию для быстрого развертывания:

```bash
# Запустить все сервисы (приложение + PostgreSQL)
docker-compose up -d

# Остановить все сервисы
docker-compose down

# Просмотреть логи
docker-compose logs -f app

# Пересобрать и перезапустить
docker-compose up -d --build
```

**Важно:** 
- Docker Compose автоматически загружает переменные окружения из файла `.env`
- Убедитесь, что файл `.env` создан и заполнен перед запуском
- База данных PostgreSQL будет доступна на `localhost:5432` внутри Docker сети

## 📦 Production Deployment

См. `Dockerfile` и `docker-compose.yml` для настройки production окружения.

### Структура проекта

```
alfaGPT/
├── app/                    # Next.js App Router
│   ├── [locale]/           # Локализованные страницы
│   │   ├── (auth)/        # Страницы авторизации
│   │   ├── (main)/        # Основные страницы приложения
│   │   └── (playwright)/  # Вспомогательные страницы для тестирования
│   └── api/               # API routes
├── components/            # React компоненты
│   ├── analytics/         # Компоненты аналитики
│   ├── auth/              # Компоненты авторизации
│   ├── business/          # Компоненты управления бизнесами
│   ├── chat/              # Компоненты чата
│   ├── dashboard/          # Компоненты дашборда
│   ├── employees/          # Компоненты управления сотрудниками
│   ├── finance/            # Компоненты финансов
│   ├── legal/              # Компоненты юридического чата
│   ├── operations/         # Компоненты операций
│   └── ui/                 # Переиспользуемые UI компоненты
├── lib/                   # Утилиты и библиотеки
│   ├── ai.ts              # Интеграция с ИИ провайдерами
│   ├── auth.ts            # Логика авторизации
│   ├── email.ts           # Отправка email
│   └── prisma.ts          # Prisma клиент
├── messages/              # Файлы локализации
│   ├── en.json            # Английский
│   ├── ru.json            # Русский
│   └── kz.json            # Казахский
├── prisma/                # Prisma схема и миграции
│   ├── schema.prisma      # Схема базы данных
│   └── seed.ts            # Тестовые данные
└── e2e/                   # Playwright тесты
```

## 🔧 Дополнительная информация

### Импорт транзакций

Для импорта транзакций из Excel используйте формат:
- **Формат файла**: XLSX
- **Колонки**: Date, Type (income/expense), Amount, Category, Description
- **Примеры файлов**: `public/samples/transactions-sample-1.xlsx`

Функция доступна во вкладке "Финансы" → "Транзакции" → кнопка "Импорт данных".

### Экспорт аналитики

Аналитические данные можно экспортировать в Excel:
- Доступно в разделе "Аналитика"
- Включает все транзакции за выбранный период
- Формат: XLSX с детализацией по категориям

### Почему OpenRouter вместо локального развертывания?

- **Готовая инфраструктура** — не нужно настраивать GPU, драйверы, обновления
- **Низкая латентность** — оптимизированные серверы с балансировкой нагрузки
- **Гибкость моделей** — легко переключаться между моделями через один API
- **Экономия** — платите только за использование, без затрат на железо
- **Быстрый старт** — интеграция за минуты, без MLOps-пайплайнов

Локальное развертывание имеет смысл только для:
- Строгих требований к конфиденциальности данных
- Офлайн-режима работы
- Очень специфичных требований к latency
