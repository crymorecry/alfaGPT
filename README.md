# alfaGPT (Volency)

Универсальный бизнес-помощник с ИИ для управления всеми аспектами бизнеса в одном месте. Построен на Next.js 13 с TypeScript, Prisma и PostgreSQL.

## ✨ Реализованные функции

### 🔐 Аутентификация
- Авторизация через email с кодом подтверждения
- Безопасное хранение токенов
- Сессии пользователей

### 💬 Чат с ИИ
- **Общий чат** — универсальный ИИ-помощник для бизнес-задач
- **Юридический чат** — специализированный чат для юридических консультаций
- История всех диалогов с сохранением в базе данных
- Очистка истории чата

### 💰 Финансы
- **Транзакции** — учет доходов и расходов с категориями
- **Платежи** — управление платежами с отслеживанием статусов
- **Регулярные расходы** — настройка периодических платежей
- Визуализация финансовых данных с графиками
- Фильтрация по датам и категориям

### 👥 Сотрудники
- Управление сотрудниками бизнеса
- Учет рабочих дней сотрудников
- Настройка ставок и расписания работы
- История работы по дням

### 📋 Операции
- **Задачи** — управление задачами с приоритетами и дедлайнами
- **Напоминания** — напоминания о важных событиях
- Фильтрация и сортировка задач
- Отслеживание статусов

### 📊 Аналитика и Дашборд
- Обзор всех активностей бизнеса
- Календарь событий и задач
- Визуализация данных
- ИИ-анализ данных

### 🏢 Управление бизнесами
- Создание и управление несколькими бизнесами
- Переключение между бизнесами
- Настройка информации о бизнесе (адрес, карты)

## 🛠 Технологии

- **Next.js 13** — React фреймворк с App Router
- **TypeScript** — строгая типизация
- **Prisma 7** — современный ORM для работы с БД
- **PostgreSQL** — реляционная база данных
- **Chakra UI** — компоненты интерфейса
- **Tailwind CSS** — утилитарная стилизация
- **next-intl** — интернационализация (русский язык)
- **Recharts** — графики и визуализация данных
- **OpenRouter API** — интеграция с различными ИИ моделями
- **Lucide React** — иконки
- **date-fns** — работа с датами

## 🚀 Быстрый старт

### Требования
- Node.js 20+
- Docker и Docker Compose (для локальной БД)
- Переменные окружения (см. ниже)

### Установка

```bash
# Клонируем репозиторий
git clone <repo-url>
cd alfaGPT

# Устанавливаем зависимости
npm install

# Запускаем PostgreSQL в Docker
npm run db:up

# Генерируем Prisma клиент
npx prisma generate

# Применяем миграции базы данных
npm run db:migrate

# (Опционально) Заполняем тестовыми данными
npm run seed

# Запускаем dev сервер
npm run dev
```

Открой [http://localhost:3000](http://localhost:3000) в браузере.

### 🗄 Управление базой данных

```bash
# Запустить PostgreSQL
npm run db:up

# Остановить PostgreSQL
npm run db:down

# Перезапустить PostgreSQL
npm run db:restart

# Просмотреть логи БД
npm run db:logs

# Применить миграции
npm run db:migrate

# Открыть Prisma Studio (веб-интерфейс для БД)
npm run db:studio

# Сбросить БД (удалить все данные и применить миграции заново)
npm run db:reset
```

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# База данных (для локальной разработки через Docker)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/alfaGPT?schema=public"

# OpenRouter API для ИИ
OPENROUTER_API_KEY="sk-or-v1-..."
OPENROUTER_MODEL="openai/gpt-oss-20b:free"

# Email для отправки кодов авторизации
NAME_EMAIL="your-email@example.com"
PASS_EMAIL="your-email-password"

# Telegram (опционально)
TELEGRAM_BOT_TOKEN="your-telegram-token"
TELEGRAM_CHAT_ID="your-chat-id"

# URL приложения
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

**Важно:** Для локальной разработки используйте Docker Compose для поднятия PostgreSQL. База данных будет доступна на `localhost:5432` с учетными данными:
- **Пользователь:** `postgres`
- **Пароль:** `postgres`
- **База данных:** `alfaGPT`

## 📜 Скрипты

### Основные команды
- `npm run dev` — запуск dev сервера на http://localhost:3000
- `npm run build` — сборка для продакшена
- `npm run start` — запуск продакшен сервера
- `npm run lint` — проверка кода линтером
- `npm run seed` — заполнение БД тестовыми данными

### Команды для базы данных
- `npm run db:up` — запустить PostgreSQL в Docker
- `npm run db:down` — остановить PostgreSQL
- `npm run db:restart` — перезапустить PostgreSQL
- `npm run db:logs` — просмотреть логи БД
- `npm run db:migrate` — применить миграции
- `npm run db:migrate:deploy` — применить миграции в продакшене
- `npm run db:studio` — открыть Prisma Studio (веб-интерфейс для БД)
- `npm run db:reset` — сбросить БД (удалить все данные)

## 🐳 Docker и продакшен

### Локальная разработка

Для локальной разработки используйте `docker-compose.yml`:

```bash
# Запустить только PostgreSQL
npm run db:up
```

### Продакшен деплой

Для продакшена используйте `docker-compose.prod.yml`:

```bash
# Создайте .env файл с переменными окружения
# Затем запустите:
docker-compose -f docker-compose.prod.yml up -d

# Применить миграции в продакшене
docker-compose -f docker-compose.prod.yml exec app npm run db:migrate:deploy

# Просмотреть логи
docker-compose -f docker-compose.prod.yml logs -f

# Остановить
docker-compose -f docker-compose.prod.yml down
```

**Важно:** 
- Не храните секреты в dockerfile! Используйте переменные окружения через `.env` файл
- Для продакшена создайте отдельный `.env.prod` файл с реальными значениями
- Никогда не коммитьте `.env` файлы в git

## 📁 Структура проекта

```
alfaGPT/
├── app/
│   ├── [locale]/              # Роутинг с локализацией
│   │   ├── (auth)/            # Страницы аутентификации
│   │   │   └── login/         # Страница входа
│   │   └── (main)/            # Основные страницы приложения
│   │       ├── analytics/     # Аналитика
│   │       ├── chat/          # Чат с ИИ
│   │       ├── employees/     # Сотрудники
│   │       ├── finance/       # Финансы
│   │       ├── operations/     # Задачи и напоминания
│   │       └── page.tsx       # Главная страница (дашборд)
│   └── api/                   # API роуты
│       ├── auth/              # Аутентификация
│       ├── analytics/         # Аналитика
│       ├── businesses/        # Бизнесы
│       ├── calendar/          # Календарь
│       ├── chat/              # Чат API
│       ├── employees/          # Сотрудники
│       ├── payments/           # Платежи
│       ├── recurring-expenses/ # Регулярные расходы
│       ├── reminders/          # Напоминания
│       ├── tasks/              # Задачи
│       └── transactions/      # Транзакции
│
├── components/                 # React компоненты
│   ├── analytics/             # Компоненты аналитики
│   ├── auth/                  # Авторизация
│   ├── business/              # Управление бизнесами
│   ├── chat/                  # Чат интерфейс
│   ├── dashboard/             # Дашборд
│   ├── employees/             # Сотрудники
│   ├── finance/               # Финансовые компоненты
│   ├── legal/                 # Юридический чат
│   ├── operations/            # Задачи и напоминания
│   ├── ui/                    # UI компоненты
│   └── user/                  # Профиль пользователя
│
├── lib/                        # Утилиты и хелперы
│   ├── auth.ts                # Логика аутентификации
│   ├── email.ts               # Отправка email
│   ├── middleware-auth.ts     # Middleware для проверки auth
│   └── prisma.ts              # Prisma клиент
│
├── prisma/                     # База данных
│   ├── schema.prisma          # Схема Prisma
│   └── migrations/            # Миграции БД
│
├── generated/                  # Сгенерированные файлы
│   └── prisma/                # Prisma клиент
│
├── messages/                   # Локализация
│   └── ru.json                # Русские переводы
│
└── public/                     # Статические файлы
```

## 🗄 Модели базы данных

- **User** — пользователи системы
- **Business** — бизнесы пользователей
- **Employee** — сотрудники бизнеса
- **EmployeeDay** — рабочие дни сотрудников
- **ChatMessage** — сообщения чата
- **Transaction** — финансовые транзакции
- **Payment** — платежи
- **RecurringExpense** — регулярные расходы
- **Task** — задачи
- **Reminder** — напоминания
- **AuthToken** — токены авторизации

## 🎨 Особенности

- **Темная/светлая тема** — переключение через ThemeChanger
- **Адаптивный дизайн** — работает на всех устройствах
- **Локализация** — полная поддержка русского языка
- **Безопасность** — защищенные API роуты с проверкой авторизации
- **Валидация** — проверка данных на клиенте и сервере

## 🔮 Планы на будущее

### Функциональность
- [ ] Экспорт данных в Excel/PDF
- [ ] Интеграция с банками для автоматического импорта транзакций
- [ ] Уведомления в браузере/email
- [ ] Командная работа — несколько пользователей
- [ ] Роли и права доступа
- [ ] Интеграция с календарями (Google Calendar, Outlook)
- [ ] Поиск по всем данным
- [ ] Шаблоны для быстрого создания задач/транзакций
- [ ] Автоматические категории для транзакций на основе ИИ
- [ ] Прогнозирование доходов/расходов

### Технические улучшения
- [ ] Unit и E2E тесты
- [ ] CI/CD пайплайн
- [ ] Кэширование данных (Redis)
- [ ] WebSocket для real-time обновлений
- [ ] PWA поддержка
- [ ] Мониторинг и логирование
- [ ] Rate limiting для API
- [ ] Двухфакторная аутентификация
- [ ] OAuth провайдеры

## 📝 Лицензия

Приватный проект

## 👨‍💻 Разработка

Проект активно разрабатывается. Все функции протестированы и работают.

---

**Сделано с ❤️ для управления бизнесом**
